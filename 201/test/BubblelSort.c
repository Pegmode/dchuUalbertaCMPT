//inclass lecture 10 bubble sort example
//not finished


int main(){

  int i,n;

  printf("Enter the legnth of the array: ");
  scanf("%d", &n);
  int a[n];
  printf("Enter %i integers to be sorted: ", n);
  for (int i = 0; i < n; i++){
    scanf("%i",&a[i]);
  }
  for(int i = 0; i < n-1; i++){
    for(int j = 0; j < n-1;j++){
      if(a[j] > a[j+1];j++){
        a[j]
      }
    }
  }
  return 0;
}
