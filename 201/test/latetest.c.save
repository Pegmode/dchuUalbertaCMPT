#include <stdlib.h>
#include <stdio.h>
#define SIZE 40000

int main(){

	for(int i = 0;;i++){
		int * = malloc(SIZE * sizeof(int));
		for(int j = 0; j < SIZE;j++){			
		}
		;
		printf("%i",i);
	}

	return 0;
}
